% Author: Julien Cubizolles
% Public domain

\RequirePackage{pgfplots}
\pgfkeys{/pgf/number format/.cd, set decimal separator={,}, 1000 sep={ }}

\pgfplotsset{compat=newest}

\pgfplotsset{%
  colormap = {tango}{color=(DarkSkyBlue) color=(LightPlum)}}

\colorlet{ExtraTickColor}{DarkPlum}

\pgfplotsset{extra tick style={color=ExtraTickColor}
}

\pgfplotscreateplotcyclelist{monochrome}{%
        {black},%
        {black,dashed},%
        {black,densely dotted},%
        {DarkGray},%
        {DarkGray,dashed},%
        {DarkGray,densely dotted},%             
        {Gray,dashed},%
        }

\pgfplotscreateplotcyclelist{polychrome}{%
        {SkyBlue},%
        {Plum,dashed},%
        {Chameleon, densely dotted},%
        {SkyBlue,dashed},%
        {Plum,dashed},%
        {Chameleon,dashed},%
        {SkyBlue,dotted},%
        {Plum,dotted},%             
        {Chameleon,dotted},%
        }       

\pgfplotsset{%
  petit graphe/.style = {%
    width = 7cm},
  tres petit graphe/.style = {%
    width = 6cm},
}    

\pgfplotsset{every axis/.append style=
                   {major tick style={thick}},
                    major grid style ={thick}}
\pgfplotsset{%
  courbe coin/.style = {%
    axis x line = bottom,
    axis y line = left,
    near ticklabel align=center,
    every axis y label/.append style=      
      {at={(ticklabel cs:0.5)},anchor=near yticklabel},
    every axis x label/.append style=
       {at={(axis description cs:1,0)},anchor = south west},
    axis line style={-Latex[round]}
       }
}

\pgfplotsset{%
  courbe centre/.style = {%
    axis lines=center,
    near ticklabel align=center,
    every axis y label/.append style=      
      {% at={(axis description cs:0.5,1)}, 
      anchor=south},
    every axis x label/.append style=
       {at={(ticklabel cs:1)}, anchor=near xticklabel},
    axis line style={-Latex[round]}
       }
}

\pgfplotsset{%
  courbe boite/.style = {%
    axis lines = box,
    every axis y label/.append style=      
      {at={(ticklabel cs:.5)},anchor = near yticklabel},
    every axis x label/.append style=
       {at={(ticklabel cs:.5)}, anchor = near xticklabel},
       }
}

\pgfplotsset{%
   IsothermesAndrews/.style={
     axis lines = box,
     legend style={at={(axis description cs:.95,.95)},anchor=north east},
     xlabel = $v(\si{\meter\cubed\per\kilo\gram})$,
     ylabel = $P(\si{\bar})$,
     minor x tick num=3,
     minor y tick num=4, 
     xtick = {0,.002,...,.01},
     ytick = {0,50,...,200},
     major grid style={very thin},
     minor grid style={thin},
     grid=both,
     every axis plot/.append style={
           unbounded coords=jump,
           draw,
           mark = none,
     }
  }
}

% \pgfplotsset{every axis plot/.append style = {thick},
%              every axis plot/.append style = {mark = none},}

\pgfplotsset{points de mesure/.style= {%
                    every axis plot/.style = {%
                    mark = +,     
                    only marks,
    }}}

\pgfplotsset{guide oeil/.style= {%
                    every axis plot post/.append style = {%
                    mark = +,
                    mark scale = 2,
                    very thin,
    }}}
